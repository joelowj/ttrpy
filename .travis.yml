language: python

python:
  - "3.6"

notifications:
  email: false

before_install:
  - sudo apt-get update
  - sudo apt-get -y install python-pip
  - pip install --upgrade pip

install:
  - pip install -r requirements.txt

script:
  - python -m tests.test_trends -v
  - python -m tests.test_momentums -v

branches:
  only:
    - master
